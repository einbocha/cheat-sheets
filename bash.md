# Bash

- create nested folder structure: ```mkdir -p folder/subfolder/subsubfolder```
- ssh into remote server: ```ssh user@remote_server```
- copy file to remove server (scp from to): ```scp file user@remote_server:/path/to/remote/location```